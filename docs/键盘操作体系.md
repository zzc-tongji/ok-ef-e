# 项目键盘操作体系说明

本项目支持高度可配置的游戏键盘操作，所有热键均可通过配置自定义，支持多场景（通用、工业、战斗）下的快捷键映射。本文档详细介绍键盘操作的设计、默认键位、扩展方式及开发注意事项。

---

## 1. 按键配置体系概览

- **热键统一管理**：所有键盘操作均通过 `KeyConfigManager` 管理，支持用户自定义和默认值。
- **三大类热键**：
  - 通用（common）：如地图、交互、背包等
  - 集成工业（industry）：如工业规划、放置传送带等
  - 战斗（combat）：如连携技等
- **国际化支持**：所有键名均有 i18n 支持，便于多语言适配。

---

## 2. 默认键位表

### 通用（DEFAULT_COMMON_KEYS）

| 配置键名           | 默认按键   | 说明     |
|--------------------|------------|----------|
| Dodge Key          | lshift     | 闪避     |
| Jump Key           | space      | 跳跃     |
| Interact Key       | f          | 交互     |
| Backpack Key       | b          | 背包     |
| Valuables Key      | n          | 贵重品   |
| Team Key           | u          | 队伍     |
| Operator Key       | c          | 干员     |
| Mission Key        | j          | 任务     |
| Track Key          | v          | 追踪     |
| Map Key            | m          | 地图     |
| Baker Key          | h          | 帝江号   |
| Mail Key           | k          | 邮件     |
| Handbook Key       | f8         | 手册     |
| Recruitment Key    | f9         | 招募     |
| Quick Tool Key     | r          | 快捷工具 |

### 集成工业（DEFAULT_INDUSTRY_KEYS）

| 配置键名             | 默认按键   | 说明       |
|----------------------|------------|------------|
| Industry Plan Key    | t          | 工业规划   |
| Place Belt Key       | e          | 放置传送带 |
| Place Pipeline Key   | q          | 放置管道   |
| Equipment List Key   | z          | 设备列表   |
| Overview Mode Key    | capslock   | 全局视图   |
| Storage Mode Key     | x          | 仓库模式   |
| Area Build Key       | y          | 区域建造   |
| Blueprint Key        | f1         | 蓝图       |
| Product Icon Toggle Key | f4      | 产品图标切换 |

### 战斗（DEFAULT_COMBAT_KEYS）

| 配置键名        | 默认按键 | 说明     |
|-----------------|----------|----------|
| Link Skill Key  | e        | 连携技   |

---

## 3. 按键调用方式

### 3.1 业务层接口

- `press_game_key(key, key_type='common', ...)`：发送指定类型的热键，自动适配用户自定义。
- `press_key(key, **kwargs)`：发送通用按键（如地图、交互等）。
- `press_industry_key(key, **kwargs)`：发送工业专用热键。
- `press_combat_key(key, **kwargs)`：发送战斗专用热键。

### 3.2 按键映射流程

1. 业务层调用如 `press_key('m')`。
2. 通过 `KeyConfigManager.resolve_common_key('m')` 查找逻辑键名对应的配置键名。
3. 若用户有自定义，则取自定义值，否则用默认值。
4. 最终通过底层 `send_key`进行物理按键模拟。

---

## 4. 用户自定义与国际化

- 用户可通过配置文件自定义所有热键。
- 支持多语言键名，详见 `i18n/` 目录下各语言包。

---

## 5. 开发注意事项

- **严禁使用send_key**，必须通过 `press_key` `press_industry_key` `press_combat_key` 调用。
- 若热键可自定义，则不可用作模板图片（否则模板匹配会因用户自定义失效）。
- 详细开发规范见 `docs/dev/DEVELOPMENT.md`。

---

## 6. 确认使用 send_key 的例外位置

以下位置经过确认，因特殊原因直接使用了底层 `send_key` / `send_key_down` / `send_key_up`，均已在代码处添加注释说明：

| 文件 | 行号 | 按键 | 原因 |
|------|------|------|------|
| `src/tasks/BaseEfTask.py` | `press_key` / `press_industry_key` / `press_combat_key` 方法体 | 动态 | 这三个方法**本身**就是对 `send_key` 的封装，合法调用 |
| `src/tasks/BaseEfTask.py` | `click_with_alt` 方法 | `alt` | `alt` 为系统修饰键，用于 alt+点击操作，非游戏可配置热键 |
| `src/tasks/BaseEfTask.py` | `skip_dialog` 方法 | `esc` | `esc` 为系统通用退出键，非游戏可配置热键 |
| `src/tasks/AutoCombatLogic.py` | 普通技能释放逻辑 | 动态技能键 | 技能键为游戏固定不可配置键，不经过 `KeyConfigManager` 管理 |
| `src/tasks/mixin/battle_mixin.py` | `use_ult` 方法 | `1`/`2`/`3`/`4` | 终极技键位为游戏固定不可配置键，不经过 `KeyConfigManager` 管理 |
| `src/tasks/mixin/liaison_mixin.py` | 干员交互导航回调 / `click_chat_box` 方法 | `alt` | `alt` 为系统修饰键，用于 alt+点击干员聊天框，非游戏可配置热键 |
| `src/tasks/daily/daily_routine_mixin.py` | 每日奖励领取逻辑 | `esc` | `esc` 为系统通用退出键，非游戏可配置热键 |
| `src/tasks/daily/daily_buy_mixin.py` | 购买逻辑 | `esc` | `esc` 为系统通用退出键，非游戏可配置热键 |
| `src/tasks/AutoSkipDialogTask.py` | `run` 方法 | `esc` | `esc` 为系统通用退出键，非游戏可配置热键 |
| `src/tasks/WarehouseTransferTask.py` | 仓库切换检测逻辑 | `esc` | `esc` 为系统通用退出键，非游戏可配置热键 |
| `src/tasks/WarehouseTransferTask.py` | `_ctrl_click` 方法 | `LCONTROL` | `LCONTROL` 为系统修饰键，用于 ctrl+点击多选，非游戏可配置热键 |

> **说明**：所有例外均为系统级按键（`esc`、`alt`、`LCONTROL`）或游戏固定不可配置键（技能键、终极技键），这类按键不经过 `KeyConfigManager` 是合理的。新增直接 `send_key` 调用时，必须在代码处加注释并补充本表。

---

## 7. 参考

- [热键配置器代码](../src/interaction/KeyConfig.py)
- [按键函数封装示例](https://github.com/AliceJump/ok-end-field/blob/69676fe978ed243f61e10147839a35f0716c2fef/src/tasks/BaseEfTask.py#L173)
- [config配置](https://github.com/AliceJump/ok-end-field/blob/69676fe978ed243f61e10147839a35f0716c2fef/src/config.py#L45)
- 开发文档：`docs/dev/API.md`, `docs/dev/DEVELOPMENT.md`
- 国际化资源：`i18n/`

---

如需扩展新热键，请在 `KeyConfigManager` 及相关默认键表中补充，并完善 i18n 资源。